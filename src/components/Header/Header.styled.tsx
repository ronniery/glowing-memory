import styled from '@emotion/styled';
import { colors } from '../../utils/constants';

export const IconWrapper = styled('div')({
  border: `2px solid ${colors.lightCornflowerBlue}`,
  borderRadius: '8px',
  padding: '8px',
  display: 'flex',
  justifyContent: 'center',
  alignContent: 'center',
});
